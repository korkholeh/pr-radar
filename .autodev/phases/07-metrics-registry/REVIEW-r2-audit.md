# Audit of the round-2 fixes — phase 7

**Verdict:** approve

The MAJOR from REVIEW-r2 is genuinely fixed in the product: mark_dirty() now nominates the reverted PR's merge day from reverts_pr_id, and the sync pipeline captures the pre-derive reverts_pr_id and passes it as previous_reverts_pr_id so a repointed or cleared revert link also dirties the day it used to target. Both new tests in test_data_version.py fail without the fix (the old code produced only the revert PR's own days). Verified reverts_pr is the forward FK behind revert_rate's reverted_by__isnull=False numerator, so the fix targets the right day. Two of the three MINORs are fixed and each carries a regression test: the DirtyDay race is closed by bumping marked_at on every mark_dirty() via update_or_create and deleting the marker only on an exact (id, marked_at) match — confirmed the bump really persists despite auto_now_add, since the new race test would fail otherwise; the CONFIGURATION.md paragraph is moved below the settings table so the policy/churn/ui/export rows render again. The NIT registry gate was strengthened, not weakened (glob-discovered modules, match scoped to get_metric(...) call sites). No test was deleted, skipped or loosened; the suite grew by exactly the three new tests. On my own run the full suite (1126 tests) and the complete CLAUDE.md lint gate — ruff check, ruff format --check, mypy, makemigrations --check --dry-run, manage.py check — all pass. Nothing contradicts the spec, ARCHITECTURE.md, ADR 0007 or a logged decision; the only other marked_at reader is the admin list_display. The one gap is bookkeeping: the deferred restricted-path query-cost MINOR is disclosed in the session's structured summary but has no bullet in .autodev/DECISIONS.md, and PROGRESS.md truncates the summary before it. Since the review itself called that item not urgent and placed it at phase 9, and the rubric says not to flag work the roadmap places in a later phase, this does not block the commit.

## [MINOR] Deferral of the restricted-path query-cost MINOR is not logged in DECISIONS.md
`.autodev/DECISIONS.md`

REVIEW-r2's fourth finding (restricted-ScopeFilter read path costs 902 queries vs 7, with no bound test) was left unfixed. The fix session's structured summary states the deferral and its reasoning ("not urgent", needs restructuring the batch machinery, out of scope for a small round-2 fix), but the new `## p07-review_fix2` section of .autodev/DECISIONS.md has five bullets covering only the revert-day fix, the marked_at race fix, the CONFIGURATION.md move, the registry-gate rescope and the css manifest — none of them mentions the deferral. PROGRESS.md line 216 truncates the summary at "- MINO…", before the deferral is named. So the repo's durable record shows the deferral nowhere, and a later phase-9 session inheriting the O(days x buckets) restricted read path has no logged trace of the decision or of the review that measured it. The deferral itself is defensible — the review placed the fix at phase 9 and confirmed correctness is unaffected — so this is a record-keeping gap, not a wrong call.

**Fix:** Append one bullet to the `## p07-review_fix2` section of .autodev/DECISIONS.md, e.g.: `- [p07/review_fix2] REVIEW-r2's restricted-path query-cost MINOR (902 queries vs 7 on a 30-day/5-metric window) deferred to phase 9 — why: the review measured correct values and placed the fix at phase 9, when per-user narrowing turns on and RISKS row 10's 1.5 s budget starts to bite; fixing it needs the batch machinery reused for the restricted path, which is larger than a round-2 fix — alternatives: reuse grouped aggregates now (too large a change after review rounds are spent), sum PROJECT-scope rollups over access.project_ids with a repo-dedup step (same size).` Add a matching `django_assert_num_queries` bound for a restricted `compute()` when phase 9 does the work.
