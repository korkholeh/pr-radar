# Committed e2e surface config. A per-developer override belongs in surfaces.local.toml (gitignored).
[app]
base_url_env = "E2E_BASE_URL"
default_base_url = "http://127.0.0.1:8100"
start_command = "make e2e-up"
stop_command = "make e2e-down"
